# Pulse Stopwatch

A simple static stopwatch with tactile controls and a Three.js 3D visual layer.

## Run locally

Open `index.html` in a browser, or serve the folder with any static file server.

## Docker

```sh
docker build -t pulse-stopwatch .
docker run --rm -p 8080:80 pulse-stopwatch
```
